[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    integration: tests that load a real model or hit an external service. Slower (~10-30 s each). Skip with ``pytest -m "not integration"``.
